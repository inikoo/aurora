ALTER TABLE `Account Data` ADD `Account Production Active Parts Number` MEDIUMINT UNSIGNED NOT NULL DEFAULT '0' AFTER `Account Parts Forced not for Sale`, ADD `Account Production In Process Parts Number` MEDIUMINT UNSIGNED NOT NULL DEFAULT '0' AFTER `Account Production Active Parts Number`, ADD `Account Production Discontinued Parts Number` MEDIUMINT UNSIGNED NOT NULL DEFAULT '0' AFTER `Account Production In Process Parts Number`, ADD `Account Production Discontinuing Parts Number` MEDIUMINT UNSIGNED NOT NULL DEFAULT '0' AFTER `Account Production Discontinued Parts Number`, ADD `Account Production Parts No Products` MEDIUMINT UNSIGNED NOT NULL DEFAULT '0' AFTER `Account Production Discontinuing Parts Number`, ADD `Account Production Parts Forced not for Sale` MEDIUMINT UNSIGNED NOT NULL DEFAULT '0' AFTER `Account Production Parts No Products`;
